<resources >
    <style name="Theme.RiktaLauncher" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:statusBarColor">#CBF4F1</item>
    </style>

    <style name="Theme.RiktaLauncher.Splash" parent="Theme.SplashScreen">
        <!-- Set the background color -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Set the logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <!-- Customize the icon animation duration -->
        <item name="windowSplashScreenAnimationDuration">500</item>
        <!-- Set the status bar color -->
        <item name="postSplashScreenTheme">@style/Theme.RiktaLauncher</item>
    </style>
</resources>
